type            = "bootstrap"
schemaVersion   = 1
name            = "bootstrap-job"
relay           = "evm"
contractID      = "{{.BOOTSTRAP_CONTRACT_ADDRESS}}" # dkg contract address

[relayConfig]
chainID = "{{.CHAIN_ID}}"
fromBlock = 0